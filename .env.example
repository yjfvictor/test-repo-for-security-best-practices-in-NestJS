# Application environment (development | production | test)
NODE_ENV=development
# HTTP port (1-65535)
PORT=3000
# Optional: API key for external services (never commit real values)
# API_KEY=
# Optional: Database connection string (never commit real values)
# DATABASE_URL=
